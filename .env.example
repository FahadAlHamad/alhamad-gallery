# Database
DATABASE_URL="file:./dev.db"

# Session encryption key (must be at least 32 characters)
SESSION_PASSWORD="change-this-to-a-random-string-at-least-32-characters"

# SMTP configuration for contact form email notifications
SMTP_HOST="smtp.example.com"
SMTP_PORT="587"
SMTP_USER="your-email@example.com"
SMTP_PASS="your-email-password"

# Email address to receive enquiry notifications
CONTACT_EMAIL="admin@alhamadgallery.com"
